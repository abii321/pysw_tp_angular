export class Punto3 {
    id: number;
    url: string;
    vuelta: boolean;

    constructor(id: number, url: string, vuelta: boolean){
        this.id = id;
        this.url = url;
        this.vuelta = vuelta;
    }
}
