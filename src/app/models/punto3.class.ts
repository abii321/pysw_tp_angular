export class Punto3 {
    id: number;
    url: string;
    encontrada: boolean;

    constructor(id: number, url: string, encontrada: boolean){
        this.id = id;
        this.url = url;
        this.encontrada = encontrada;
    }
}
